# 可移植 PPT 工作区

本上下文定义 `modal/` 交付包中的核心术语，避免把工作区交付、Skill 注册和项目环境准备混为一谈。

## Language

**可移植 PPT 工作区**：
以 `modal/` 为根目录、可直接交给同事并由 Agent 打开的完整工作目录；内含项目级 Skills、入口说明和约定的输入、脚本及输出位置。
_Avoid_: Skill 压缩包、提示词目录、自动安装包

**既有目录接入**：
把可移植 PPT 工作区中的能力迁移到用户已有项目的备用模式，不是新手默认使用路径。
_Avoid_: 默认部署、自动注册

**包管理器选择**：
可移植 PPT 工作区同时支持 pnpm 和 npm，默认选择 pnpm；既有项目已使用 npm、pnpm 不可用或用户明确选择 npm 时采用 npm。每个实际工作区只能选择其中一种，并只保留对应锁文件。
_Avoid_: npm 与 pnpm 混用、双锁文件

**辅助环境安装**：
Agent 可以辅助安装版本管理器、Node.js 和 pnpm；下载程序、修改 PATH、创建系统链接、申请管理员权限或执行其他系统级变更前，必须说明影响并获得用户确认。受企业安全策略阻止时转为人工安装，完成后恢复原流程。
_Avoid_: 静默安装、无确认的系统级变更、只能手动安装

**默认运行时**：
新部署默认使用 Node.js 24 LTS 与 pnpm 11，同时兼容 Node.js 22 LTS；npm 是备选包管理器，不再把 Node.js 20 及更早版本作为新部署环境。
_Avoid_: Node.js Current 版本、已结束支持的 Node.js 版本

**依赖基线**：
工作区固定 PptxGenJS 的具体版本，不使用浮动的 `latest`；交付时预置 `package.json` 与 `.nvmrc`，首次部署根据实际选择生成且只生成一种锁文件。
_Avoid_: 浮动依赖、预置双锁文件

**首版输入范围**：
首版原生覆盖 Markdown 与 DOCX；PDF、Excel 只有在工作区包含对应转换 Skill 时才属于受支持输入，否则 Agent 必须提前报告能力缺失。
_Avoid_: 无转换能力时宣称支持所有格式、临时解析 Office 文件

**主要验收平台**：
Windows 是首版主要验收环境，macOS 与 Linux 保留环境检测和安装引导分支，但不与 Windows 享有同等首版验证承诺。
_Avoid_: 未经验证的全平台一致性承诺

**中文 Skill 规范**：
两个 `SKILL.md` 使用中文描述流程和风险；命令、路径、文件名、配置键及代码保持原文。
_Avoid_: 中英文流程规则混写、翻译命令和配置键

**迁移提示词**：
用于指导 Agent 把工作流接入用户已有项目的备用入口；不得替代可移植 PPT 工作区这一默认交付方式。
_Avoid_: 默认入口、自动改造任意目录

**环境验收**：
验证 Node.js、选定包管理器、PptxGenJS 模块加载和已有 `build.mjs` 语法，不生成测试 PPTX、PNG 或 QA 目录。
_Avoid_: 用正式 PPT 生成代替环境检查、默认视觉渲染
