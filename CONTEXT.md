# 可移植 PPT 工作区

本上下文定义 `modal/` 交付包中的核心术语，避免把工作区交付、Skill 注册和项目环境准备混为一谈。

## Language

**新手手册**：
当前可移植 PPT 工作区的专用操作手册，面向第一次使用本工作区且没有技术背景的用户。
_Avoid_: 通用 AI 制作 PPT 教程、跨工作区操作规范

**可移植 PPT 工作区**：
以 `modal/` 为根目录、可直接交给同事并由 Agent 打开的完整工作目录；内含项目级 Skills、入口说明和约定的输入、脚本及输出位置。
_Avoid_: Skill 压缩包、提示词目录、自动安装包

**既有目录接入**：
把可移植 PPT 工作区中的能力迁移到用户已有项目的备用模式，不是新手默认使用路径。
_Avoid_: 默认部署、自动注册

**包管理器选择**：
可移植 PPT 工作区同时支持 pnpm 和 npm，默认选择 pnpm；既有项目已使用 npm、pnpm 不可用或用户明确选择 npm 时采用 npm。每个实际工作区只能选择其中一种，并只保留对应锁文件。
_Avoid_: npm 与 pnpm 混用、双锁文件

**辅助环境安装**：
Agent 可以辅助安装版本管理器、Node.js 和 pnpm；下载程序、修改 PATH、创建系统链接、申请管理员权限或执行其他系统级变更前，必须说明影响并获得用户确认。受企业安全策略阻止时转为人工安装，完成后恢复原流程。
_Avoid_: 静默安装、无确认的系统级变更、只能手动安装

**默认运行时**：
新部署默认使用 Node.js 24 LTS 与 pnpm 11，同时兼容 Node.js 22 LTS；npm 是备选包管理器，不再把 Node.js 20 及更早版本作为新部署环境。
_Avoid_: Node.js Current 版本、已结束支持的 Node.js 版本

**依赖基线**：
工作区固定 PptxGenJS 的具体版本，不使用浮动的 `latest`；交付时预置 `package.json` 与 `.nvmrc`，首次部署根据实际选择生成且只生成一种锁文件。
_Avoid_: 浮动依赖、预置双锁文件

**首版输入范围**：
首版原生覆盖 Markdown 与 DOCX；PDF、Excel 只有在工作区包含对应转换 Skill 时才属于受支持输入，否则 Agent 必须提前报告能力缺失。
_Avoid_: 无转换能力时宣称支持所有格式、临时解析 Office 文件

**主要验收平台**：
Windows 是首版主要验收环境，macOS 与 Linux 保留环境检测和安装引导分支，但不与 Windows 享有同等首版验证承诺。
_Avoid_: 未经验证的全平台一致性承诺

**中文 Skill 规范**：
两个 `SKILL.md` 使用中文描述流程和风险；命令、路径、文件名、配置键及代码保持原文。
_Avoid_: 中英文流程规则混写、翻译命令和配置键

**中间产出**：
`intermediate/` 目录存放分阶段生成的契约文件，默认包含 `lecture.md` 与 `storyboard.md`，与工作区根目录、`input/`、`output/` 相互独立，避免阶段产物和项目结构混放。用户明确指定其他路径时遵从用户。
_Avoid_: 把 lecture、storyboard 直接放在工作区根目录

**内容稿**：
默认文件为 `intermediate/lecture.md`，用于记录从原始材料整理出的事实、数据与结论。
_Avoid_: 页面稿、演示文稿

**页面稿**：
默认文件为 `intermediate/storyboard.md`，用于约定演示文稿的页面结构、内容布局与演讲备注。
_Avoid_: 内容稿、施工图纸

**自动版**：
由构建脚本生成、文件名带 `.generated.pptx` 的可编辑演示文稿，可以被后续重新生成覆盖。
_Avoid_: 成品、终稿

**人工定稿版**：
经人工检查或调整后另存、文件名带 `.final.pptx` 的正式交付演示文稿，不属于自动覆盖目标。
_Avoid_: 自动版、生成版

**参考案例**：
保存在 `examples/` 中、包含原始材料、内容稿、页面稿与构建脚本的完整实践样例，用于说明工作流并支持复现，不作为新任务的默认内容模板。
_Avoid_: 当前任务产物、空白模板

**迁移提示词**：
`prompt/migration-prompt.md` 用于指导 Agent 把工作流接入用户已有项目的备用入口；不得替代可移植 PPT 工作区这一默认交付方式。
_Avoid_: 默认入口、自动改造任意目录

**环境验收**：
验证 Node.js、选定包管理器、PptxGenJS 模块加载和已有 `build.mjs` 语法，不生成测试 PPTX、PNG 或 QA 目录。
_Avoid_: 用正式 PPT 生成代替环境检查、默认视觉渲染

## 数据服务规划单页语言

**业务场景**：
七大主业中用于说明数据服务实际应用方向的业务分类，是项目关联的展示单元。
_Avoid_: 项目、产品类别

**项目**：
承接业务场景的具名生产应用对象，统一涵盖产品、平台或建设项目；未获得正式类型资料前不再细分对象类型。
_Avoid_: 默认把所有对象称为平台、默认把所有对象称为产品

**场景—项目关联**：
项目与业务场景之间的多对多承接关系，同一项目可以出现在多个业务场景下；关联只说明生产应用关系，不代表覆盖场景全部需求或已经产生量化成效。
_Avoid_: 一项目一场景、项目全覆盖、成效证明

**业务场景落地**：
已有生产环境项目与业务场景建立实际应用关联；页面使用该词时不再逐项重复“已投产”，也不据此声称产生量化成效。
_Avoid_: 规划中场景、项目成效证明

**未列项目场景**：
单页中保留业务场景标题但未展示具体项目名称的场景，只表示本页未建立具名关联，不表示尚未建设或没有生产应用。
_Avoid_: 未落地场景、待建设场景、空白场景

**原生重建**：
依据已确认的内容稿与页面稿，使用 PowerPoint 原生文字、形状和连接线重新实现页面；不把源页截图作为正文背景，也不直接修改 PPTX 内部 XML。
_Avoid_: 截图覆盖、XML 补丁、不可编辑复刻

**视觉 QA**：
把自动版渲染为独立 PNG 检查产物，用于核对换行、裁切、重叠、字号和同层级样式；PNG 只用于检查，不替代自动版或人工定稿版。
_Avoid_: 把截图当成最终 PPT、用包结构检查代替视觉检查
