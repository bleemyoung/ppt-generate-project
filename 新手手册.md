# 新手手册：用 AI 把材料做成可编辑 PPT

本手册根据一次真实实践（把《随身行页面梳理分析报告》做成 14 页可编辑 PPT）整理。你不懂代码也没关系：**你只负责提供材料、回答几个问题、检查做好的 PPT，其余由 Agent 完成。**

---

## 一、这套流程能做什么

给你一份材料（Word 文档或 Markdown 文字），就能得到：

- 一份**内容稿**（叫 `lecture.md`）：把材料整理成"讲了什么、有哪些数据和结论"。
- 一份**页面稿**（叫 `storyboard.md`）：规划"每一页放什么、长什么样"。
- 一份**成品 PPT**（自动生成，可在 PowerPoint 里继续编辑）。

最重要的特点：**改起来方便、能反复重新生成、别人也能接手**。这比让 AI 一次性吐一个 PPT 文件更可靠。

## 二、工作区里都有什么（你只需要认识三个地方）

| 文件夹 | 放什么 | 你需要做什么 |
| --- | --- | --- |
| `input/` | 你的原始材料 | 把材料放这里（或告诉 Agent 材料位置） |
| `intermediate/` | 中间文件：内容稿、页面稿 | 只读、确认内容，不用管格式 |
| `output/` | 做好的 PPT | 到这里打开检查、另存定稿 |
| `prompt/` | 提示词模板 | 抄一段话发给 Agent 用 |

其余（`scripts/`、`.agents/`、`package.json` 等）是工具和规则，**你不用碰**。

## 三、开始前的一句话

把本工作区目录交给 AI 助手（Agent），然后从 `prompt/quick-start.md` 里复制"普通用户提示词"，把里面 `<文件或目录>` 换成你的材料位置，发给它。你也可以直接说：

> 请先阅读当前工作区的 AGENTS.md，按照里面的 PPT 工作流处理。
> 材料：<你的材料文件或目录>
> 我没有技术背景，请用通俗语言引导我完成必要确认。

## 四、完整过程（一共 8 步，照着走就行）

### 第 1 步：准备环境（只用做一次）

Agent 会先检查电脑上缺不缺工具（Node.js、安装器、Python 等）。它只会**检查**，不会乱动你的电脑。

- 需要安装时，它会先告诉你"要装什么、会改动什么、要不要联网"，**你确认后它才动手**。
- 你只需要回答：同意 / 不同意 / 改成手动安装。
- 安装完它会汇报一个结果表（Node 版本、包管理器、PptxGenJS、Python 都就绪 = 完全就绪）。

> 现实例子：本机检测到 Node 24、pnpm 11、Python 3.11 都正常，只缺项目的 PptxGenJS 和 DOCX 转换工具，确认后一次装完，状态"完全就绪"。

### 第 2 步：确认需求（最重要的问答环节）

Agent 会一次性问你几个问题，**每个都带推荐答案**，你可以直接选"推荐"，也可以改：

1. 这份 PPT 的**用途**（汇报 / 方案 / 培训 / 复盘）
2. **听众**是谁（领导 / 同事 / 新人）
3. 希望听众听完**知道或决定什么**（核心目标）
4. **页数和时长**（约 14 页 / 10-15 分钟等）
5. **章节顺序**（按材料顺序 / 结论先行等）
6. **风格**（简洁商务风 16:9 等）

> 现实例子：案例中选择"立项/方案汇报、团队同事混合背景、约 14 页 / 10-15 分钟、按材料章节顺序、简洁商务风"。材料本身已写清楚来源，Agent 没有再问。

### 第 3 步：确认内容稿（lecture.md）

Agent 把材料整理成 `intermediate/lecture.md`，里面是**事实和结论**：背景、关键数据、阶段表、经验、对比表等。

- 你只检查一件事：**内容对不对**。数字、结论有没有错。
- 不对就说"第 X 条改成……"，对了就说"继续"。

> 现实例子：Agent 把随身行实践整理成 8 大块（背景→产出链路→中间层→分工→经验→环境→对比→结论），并声明哪些属于"正文"、哪些属于"附录不展示"。

### 第 4 步：确认页面稿（storyboard.md）

Agent 依据内容稿规划每一页：`intermediate/storyboard.md`。包含**每页标题、放什么内容、什么样子、演讲备注**，还有总页数和配色规则。

- 你确认的是"这个结构行不行、顺序对不对"。
- 之后再做细节修改会方便很多，因为这页就是"施工图纸"。

> 现实例子：最终确认 14 页：封面、议程、痛点、方案链路、阶段职责、中间层、分工、三页经验、环境、对比、适用场景、结论。

### 第 5 步：自动生成 PPT

页面稿确认后，Agent 会写生成脚本并检查，然后自动产出：

```
output/随身行PPT实践.generated.pptx
```

- 这个文件是**自动产物**，可以随时重新生成。
- 生成后 Agent 会汇报：多少页、文件多大，并确认没有生成额外的图片或 QA 目录。

### 第 6 步：人工检查（关键一步）

用 PowerPoint 打开 `output/` 里的 `.generated.pptx`，从头看一遍。重点看：

- 文字有没有**换行难看、被裁掉、重叠**；
- 表格有没有溢出；
- 页码、标题、同类型卡片是否整齐一致；
- 内容是否符合你的预期。

发现问题，**直接告诉 Agent**，例如：

> 第 10 页卡片里的文字太靠下，被卡片的底边切掉了，帮我修一下。

### 第 7 步：修改并重新生成

Agent 会先改"施工图纸"（storyboard）再改脚本，然后重新生成 PPT。

- **注意**：如果 PPT 还开着没关，会报"文件被占用"（它叫 EBUSY，不是出错了）。你要做的：**先保存并关闭 PowerPoint**，再让 Agent 重跑。
- 不要自己偷偷只改 PPT 不管 Agent —— 那样下次重新生成会把你的修改冲掉。

> 现实例子：第 10 页三张卡片正文溢出，Agent 把卡片加高、文字位置上移，更新页面稿后重新生成；期间因 PPT 开着报了占用错误，关闭后一次成功。

### 第 8 步：定稿（.final.pptx）

检查满意后，**在 PowerPoint 里"另存为"**，文件名带 `.final`：

```
output_final/随身行PPT实践.final.pptx
```

- `.final.pptx` 是**人工定稿版**，永远不会被自动覆盖；
- `.generated.pptx` 是**自动版**，随时可以重新生成；
- 如果以后还要继续改内容，优先告诉 Agent 改"施工图纸"，再让它重新生成。

## 五、常见问题（大白话版）

| 你遇到的情况 | 原因 | 怎么办 |
| --- | --- | --- |
| 重新生成时报"文件被占用 / EBUSY" | PPT 还开着 | 保存并关闭 PowerPoint 后再来一次 |
| 重新生成后我的手工修改没了 | 改在了自动版里 | 重要修改另存为 `.final.pptx`，并告诉 Agent 同步 |
| 换一台电脑 | 环境没装 | 按第 1 步让 Agent 重新检查安装，材料重新放 `input/` |
| 换一个 AI 助手 | 没关系 | 它只要会读 Markdown、能运行命令即可，同一套流程照走 |
| `.venv` 是什么 | 一个装转换工具的隐藏文件夹 | 不用管它；它保证不同电脑上工具版本一致 |

## 六、怎么和 Agent 说话

- 不知道说什么？直接复制 `prompt/quick-start.md` 里的提示词。
- 常用指令：
  - **"继续"** = 确认当前步骤，进入下一步。
  - **"产出"** = 确认页面稿没问题，开始生成 PPT。
  - **"第 X 页的 XX 有问题，帮我改"** = 描述问题，让 Agent 修。
- 想深入确认需求？可以要求 Agent"按 grilling 方式逐项拷问"需求边界。
- 想检查排版细节（换行、重叠、对齐）？可以让 Agent 做"视觉 QA"，把每一页导出成图片检查。

## 七、一句话总结

> 你只做三件事：**给材料、回答问题、检查成品**。
> 改东西永远走"告诉 Agent → 它改图纸和脚本 → 重新生成"这条路，
> 定稿永远另存 `.final.pptx`。
> 这样你的 PPT 可以反复改、随时交、别人也能接手。
